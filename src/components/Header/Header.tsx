import React from 'react';

import style from '@styles/Header.module.scss';

const Header: React.FC = () => {
  return <header className="fixed top-0 w-full p-1">Header</header>;
};

export default Header;
